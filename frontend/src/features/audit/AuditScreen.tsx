import { useOutletContext } from 'react-router-dom';import { useTranslation } from 'react-i18next';import { useAudit } from '../../api/queries';import type { DashboardContext } from '../../app/AppShell';import { Badge,EmptyState,ErrorState,Skeleton } from '../../shared/components/ui';import { useLocale } from '../../localization/runtime';
export function AuditScreen(){const{t}=useTranslation();const{locale}=useLocale();const{me,guild}=useOutletContext<DashboardContext>();const query=useAudit(me.user.discord_user_id,guild.guild_id);if(query.isLoading)return <Skeleton/>;if(query.isError)return <ErrorState retry={()=>void query.refetch()}/>;return <section><h1>{t('audit.title')}</h1>{query.data?.events.length===0&&<EmptyState messageKey="audit.empty"/>}<ol className="audit-list">{query.data?.events.map((event)=><li key={event.id}><time>{new Intl.DateTimeFormat(locale,{dateStyle:'medium',timeStyle:'short'}).format(new Date(event.occurred_at))}</time><strong>{event.event_type}</strong><span>{event.target_type} · {event.target_id}</span><Badge>{event.result_state}</Badge></li>)}</ol></section>}
