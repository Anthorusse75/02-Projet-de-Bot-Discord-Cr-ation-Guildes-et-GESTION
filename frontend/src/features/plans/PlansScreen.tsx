import { useState } from 'react'
import { useQueryClient } from '@tanstack/react-query'
import { useOutletContext } from 'react-router-dom'
import { useTranslation } from 'react-i18next'
import { apiRequest } from '../../api/client'
import { usePlans } from '../../api/queries'
import type { Plan } from '../../api/types'
import type { DashboardContext } from '../../app/AppShell'
import { Badge, Button, EmptyState, ErrorState, Progress, Skeleton } from '../../shared/components/ui'
export function PlansScreen(){const{t}=useTranslation();const{me,guild}=useOutletContext<DashboardContext>();const query=usePlans(me.user.discord_user_id,guild.guild_id);const[selected,setSelected]=useState<Plan|null>(null);const[events,setEvents]=useState<Array<{sequence:number;plan_status:string;message_key:string}>>([]);const client=useQueryClient();async function command(path:string,body?:unknown){if(!selected)return;const options:Parameters<typeof apiRequest>[1]={method:'POST'};if(path==='confirm')options.headers={'Idempotency-Key':crypto.randomUUID()};if(body!==undefined)options.body=body;await apiRequest(`/api/v1/guilds/${guild.guild_id}/plans/${selected.id}/${path}`,options);await client.invalidateQueries({queryKey:['did',me.user.discord_user_id,guild.guild_id,'plans']})}async function progress(plan:Plan){setSelected(plan);const value=await apiRequest<{events:Array<{sequence:number;plan_status:string;message_key:string}>}>(`/api/v1/guilds/${guild.guild_id}/plans/${plan.id}/progress`);setEvents(value.events)}if(query.isLoading)return <Skeleton/>;if(query.isError)return <ErrorState retry={()=>void query.refetch()}/>;return <section><h1>{t('plans.title')}</h1>{query.data?.plans.length===0&&<EmptyState messageKey="plans.empty"/>}<div className="card-grid">{query.data?.plans.map((plan)=><button type="button" className="plan-card" key={plan.id} onClick={()=>void progress(plan)}><strong>{plan.id.slice(0,8)}</strong><Badge tone={plan.status==='SUCCEEDED'?'ok':plan.status==='FAILED'?'danger':'neutral'}>{t('plans.status',{value:plan.status})}</Badge><span>{t('plans.risk',{value:plan.risk_level})}</span></button>)}</div>{selected&&<article className="detail-panel"><h2>{t('plans.progress')}</h2><Progress labelKey="plans.progress" value={Math.min(100,events.length*20)}/><div className="button-row"><Button labelKey="plans.preflight" onClick={()=>void command('validate',{expected_version:selected.state_version})}/><Button labelKey="plans.confirm" onClick={()=>void command('confirm',{expected_version:selected.state_version,plan_hash:selected.plan_hash,acknowledgement:selected.reinforced_confirmation_required?`CONFIRM DESTRUCTIVE ${selected.plan_hash}`:null})}/><Button labelKey="plans.apply" variant="primary" onClick={()=>void command('apply')}/><Button labelKey="plans.cancel" variant="danger" onClick={()=>void command('cancel')}/></div><ol>{events.map((event)=><li key={event.sequence}>{event.plan_status} · {event.message_key}</li>)}</ol></article>}</section>}
