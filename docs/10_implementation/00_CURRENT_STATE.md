# État courant

| Champ | Valeur |
|---|---|
| Current stage | `STAGE_09_COMPLETE_DRAFT_PR_OPEN` -- **restauré** depuis la régression temporaire `STAGE_09_BLOCKED_TRANSLATION_PROVIDER_UNAVAILABLE` (passe précédente) ; voir « Historique des trois remédiations » ci-dessous |
| Last completed/integrated stage | `STAGE_08_MULTILINGUAL_CONTENT_AND_TRANSLATION_TOPOLOGY` |
| Base main (point de divergence de `stage/09-campaigns`) | `c41b61ae96cdb1d767c8d924212a6466b768ed60` — le commit de `main` d'où la branche STAGE 09 a été créée ; ce n'est **pas** nécessairement le HEAD courant de `main` (voir `git rev-parse main` pour l'état à jour) |
| STAGE 08 functional tested code | `592b94bdee713cfb51e236e29cb979ba60e53ac9` |
| STAGE 08 approved final head | `42274836256d2af449678c239c2db4d8e5e6d01d` |
| STAGE 08 merge commit | `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 08 tag | `stage-08-complete` -> `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 09 branch | `stage/09-campaigns`, créée depuis `main` au commit `c41b61ae96cdb1d767c8d924212a6466b768ed60` |
| STAGE 09 HEAD (cette passe) | le commit qui contient exactement ce texte est par construction le dernier commit poussé de cette passe -- voir `git log -1 --format=%H` sur `stage/09-campaigns`, identique au head affiché sur PR #9 après le push de cette passe |
| STAGE 09 PR | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9), ouverte, non mergée, `DO NOT MERGE — EXTERNAL AUDIT REQUIRED` |
| Last migration | `0032_stage_09` ; chaîne `0013_stage_07 → … → 0021_stage_08 → 0022 → … → 0032_stage_09` (32 migrations Stage09) ; tête unique, inchangée cette passe |
| Historique des six remédiations (mêmes findings externes puis évidence empirique produit owner, PR #9) | (1) Défaut fail-open de l'adaptateur `googletrans` -- corrigé (`raise_exception=True` + vérification défensive du statut HTTP). (2) Fuite de ressources Discord dans la qualification live sur échec -- corrigée (`CleanupRegistry`, nettoyage immédiat, référencé par identité, jamais copié). (3) Mutation intermittente de placeholder côté provider (1/312 mesures `FULL_MASKED_MESSAGE` réelles) -- root-causée et corrigée par une retry bornée avec régénération de placeholders, jamais un relâchement du fail-closed. (4) Comptage sous-évalué des appels provider dans l'évidence du benchmark -- corrigé par un compteur explicite `provider_invocation_count`. (5) Le transport `googletrans` `/translate_a/single` lui-même s'est révélé spécifiquement indisponible (HTTP 429, prouvé sur deux machines du réseau du product owner) alors qu'une sonde du transport RPC Google Translate Web (`batchexecute`) a réussi (HTTP 200) depuis la même machine -- production basculée vers `GoogleTranslateRpcCampaignTranslationProvider`. (6) **Cette passe** : un audit externe a trouvé que le contrat de requête/réponse RPC choisi en (5) avait été inventé plutôt qu'empiriquement prouvé -- seuls l'endpoint et le RPC id l'étaient réellement. Le module est désormais aligné exactement sur le contrat réel prouvé par le product owner (mêmes paramètres de requête, même enveloppe `f.req`, même en-tête `Referer`, même forme de réponse). Voir `STAGE_09_HANDOFF.md` § « État actuel » pour le détail complet de chacune |
| **Transport de traduction (changé passe -5, contrat de wire corrigé cette passe)** | La conclusion antérieure (« provider `googletrans` globalement indisponible depuis ce sandbox, opérationnel depuis le réseau du product owner ») avait été affinée par une évidence empirique plus précise : le transport `/translate_a/single` spécifiquement échoue avec HTTP 429 **même depuis le réseau du product owner**, reproduit sur une seconde machine indépendante. En revanche, l'autre endpoint web non officiel de Google -- le RPC `batchexecute` (id `MkEWBc`) que `translate.google.com` utilise lui-même -- a répondu HTTP 200 avec une vraie traduction depuis la même machine où `googletrans` échoue. Production, qualification live et benchmark utilisent tous `GoogleTranslateRpcCampaignTranslationProvider` (module `did.translation.google_translate_rpc_adapter`) ; `googletrans` reste dans le dépôt uniquement comme code/dépendance historique et comparatif, jamais construit sur un chemin réel. **Cette passe** : un audit externe a établi que le contrat de requête/réponse effectivement codé pour ce module était inventé (paramètres de requête fabriqués, forme de réponse devinée), pas le contrat réel prouvé par la sonde du product owner -- seuls l'endpoint et le RPC id l'étaient. Corrigé : requête et parsing de réponse désormais alignés exactement sur ce contrat prouvé (voir `STAGE_09_HANDOFF.md` Root cause 5). Le seul blocage restant est une confirmation externe : le product owner doit rejouer le petit smoke réseau réel PUIS le benchmark canonique contre ce transport, avec le contrat désormais corrigé (voir « Commande de confirmation finale » ci-dessous) |
| STAGE 09 implementation (inchangée structurellement) | Fondations WP1-WP13, remédiation externe (17 findings), orchestration WP12 réelle, connexion runtime réelle, API HTTP complète (WP14), réconciliation de livraison câblée, surfaces d'authoring produit complètes en UI/API, matrice de qualification live Discord (WP16/mission section 20). **Résultat REQ-MSG inchangé : 31/31 `IMPLEMENTED`, 0 `PARTIALLY_IMPLEMENTED`, 0 `NOT_STARTED`** |
| Data model | Inchangé cette passe ; voir migrations `0022`-`0032_stage_09` |
| Tests status | Canonique inchangé : `validate_stage.py 09` (défaut), `08`, `--profile failure-injection`, `--profile e2e` : **PASS**. `test_stage09_rendering.py` (12) et `test_stage09_translation_benchmark_production.py` (20) inchangés. **`test_stage09_google_translate_rpc_adapter.py` intégralement réécrit cette passe** contre le contrat de wire corrigé (voir Root cause 5, `STAGE_09_HANDOFF.md`) -- fixture canonique reflétant la forme réellement capturée, frontières fail-closed testées individuellement (`[1]`, `[1][0]`, `[1][0][0]`, index 3/5 manquants, partie traduite malformée), et une nouvelle classe asserte explicitement l'enveloppe `f.req`, les paramètres de requête HTTP réels, et l'en-tête `Referer`. Toujours aucun réseau requis, `httpx.MockTransport` simule le transport HTTP -- voir « Validation canonique » de `STAGE_09_HANDOFF.md` pour le décompte PASS exact de cette passe. `ruff`/`mypy`/`check_secrets`/`git diff --check` : PASS |
| **Correction de comptage (cette passe, defect trouvé par audit externe sur la passe précédente)** | `scripts/run_translation_benchmark.py::_run_one_production()`/`_run_one()` enregistraient `retry_count`/le nombre de tentatives mais `_summarize()` dérivait `provider_invocations` de `record.segment_count` -- qui ne reflète que la mesure structurelle de la tentative GAGNANTE finale, sous-comptant silencieusement les vrais appels réseau dès qu'une reprise avait lieu (ex. tentative 1 échoue l'intégrité + tentative 2 réussit = 2 vrais appels provider, mais `segment_count` de la tentative gagnante ne vaut que 1) ou qu'une stratégie multi-segments échouait en cours de route (`segment_count=0` sur l'enregistrement d'erreur effaçait aussi les appels déjà réellement effectués). **Corrigé** : nouveau champ explicite `provider_invocation_count`, incrémenté à l'instant même où chaque appel réel à `provider.translate(...)` est tenté -- avant l'attente du résultat, donc compté même si l'appel lève, même à travers les tentatives de reprise, même pour les segments déjà appelés avant l'échec d'un segment ultérieur. `_summarize()` dérive désormais `provider_invocations` exclusivement de ce compteur explicite, y compris pour les enregistrements en erreur (précédemment ignorés par un `continue` prématuré). `segment_count` reste une mesure purement structurelle, plus jamais surchargée pour représenter le nombre d'appels réels. Validation défensive ajoutée : `max_integrity_attempts < 1` lève désormais `ValueError` explicitement (jamais un `assert` interne opaque) dans `render_field_text`/`render_message_model`/`_run_one_production`. 11 nouveaux tests déterministes le prouvent (voir ci-dessus). |
| Root-cause de la mutation de placeholder | Analysée mais non "corrigée" par un changement d'encodage : le placeholder `DIDPH{index:04d}Q{8 hex}ZH` a été transformé par le provider en un AUTRE token de même forme (un chiffre hex modifié), sur du contenu mêlant code inline et bloc de code fenced (`inline_code_and_block_mixed`, ES→FR). Le fail-closed existant a correctement rejeté ce candidat (`IntegrityViolation`) -- ce n'était PAS une brèche de sécurité, mais une lacune de disponibilité (aucune reprise). Le format de placeholder lui-même n'a pas été changé (préserverait l'invariant existant sans le justifier par la mesure) ; la reprise bornée avec régénération est la remédiation choisie, appuyée par la preuve réelle qu'un retest à 5 tentatives avec placeholders neufs a réussi 5/5 |
| Translation benchmark status | Évidence committée (ce sandbox, préservée telle quelle comme évidence historique, inchangée cette passe) : `BLOCKED`, 1248/1248 mesures en échec réel contre l'ancien transport `googletrans` -- provider indisponible ici. Évidence réelle rapportée par le product owner (non committée en JSON par cette passe : jamais fabriquée/substituée) : (a) historique -- 1950 appels réels/0 erreur contre `googletrans` opérationnel à l'époque, `FULL_MASKED_MESSAGE` 311/312 (99.68%), `PARAGRAPH_GROUPING`/`SENTENCE_GROUPING` 312/312, `NAIVE_PER_TEXT_NODE` 92.3% (contrôle négatif attendu) ; (b) récente -- ce même transport `googletrans` échoue désormais 5/5 en HTTP 429 sur deux machines ; (c) sonde ponctuelle du nouveau transport RPC, un seul appel manuel : HTTP 200, traduction réelle correcte. Aucune qualification statistique complète du transport RPC n'existe encore -- c'est l'objet de la commande de confirmation finale ci-dessous |
| **Commande de confirmation finale (product owner, réseau fonctionnel, en DEUX étapes)** | Étape 1 : `uv run pytest backend/tests/network/test_stage09_translation_network.py -m translation_network` (smoke réel exerçant directement `GoogleTranslateRpcCampaignTranslationProvider`) doit passer 5/5 en premier. Étape 2, seulement si l'étape 1 passe : `python scripts/validate_stage.py 09 --profile translation-benchmark --allow-network` -- doit rapporter `PASS` sur le bucket `PRODUCTION_FULL_MASKED_MESSAGE_WITH_RETRY` du rapport JSON (pas seulement sur le bucket comparatif `FULL_MASKED_MESSAGE` brut, qui reste sans retry), désormais mesuré contre le transport RPC |
| Discord live status | Évidence committée (ce sandbox, inchangée cette passe -- aucun accès réseau fonctionnel disponible ici pour la rejouer) : `validate_discord_live_stage09.py` 5/5 PASS ; `validate_discord_live_stage09_full_chain.py` (11 groupes) 9/11 groupes 100% PASS (rien lié à la traduction), `translation_group_provider_boundary` 100% PASS, `translation_group_did_fanout` échoue honnêtement sur ce qui dépend d'une traduction obtenue. La correction de mutation de placeholder n'a donc pas encore été vérifiée contre CE chemin live spécifique dans ce sandbox -- elle l'a été de façon déterministe (16 tests) et par le product owner au niveau benchmark |
| Nettoyage du sandbox live (passe précédente) | `CleanupRegistry` (`scripts/_stage09_cleanup_registry.py`) corrige la fuite de ressources sur échec ; `scripts/cleanup_stage09_orphan_fixtures.py` a supprimé les 50 orphelins historiques (0 échec, 0 restant vérifié). Inchangé cette passe |
| GitHub publication | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9) ouverte contre `main`, non mergée ; branche `stage/09-campaigns` poussée |
| Next stage | `STAGE_10` reste `NOT_STARTED` et interdite tant que STAGE 09 n'est pas intégrée dans `main` |
| Known limitations (toutes `EXTERNAL_ACCEPTANCE_ITEM`) | (A) **Revue sémantique humaine** : `PENDING_HUMAN_REVIEW`, le pack documente honnêtement `MACHINE_TRANSLATION_CURRENTLY_UNAVAILABLE` (depuis ce sandbox) plutôt que de fabriquer une sortie, `docs/90_handoffs/evidence/stage09/human-semantic-review-pack.md`. (B) **Confirmation du smoke réseau puis du benchmark canonique avec retry contre le nouveau transport RPC depuis un réseau fonctionnel** (clause mise à jour cette passe, remplace l'ancienne clause « provider `googletrans` indisponible ») : le product owner doit exécuter les deux commandes ci-dessus, dans l'ordre, et committer son évidence JSON réelle. (C) **Provider de traduction tiers réellement présent dans le sandbox** (distinct du chemin de traduction direct propre à DID) : `EXTERNAL_SANDBOX_CAPABILITY_NOT_AVAILABLE`, inchangé. (D) **`UNKNOWN_OUTCOME` réel non reproductible à la demande contre Discord** : `NOT_SAFELY_REPRODUCIBLE_LIVE`, inchangé. |

Le détail d'architecture, les décisions de conception, les 17 findings de remédiation externe, et le
détail pass-par-pass sont dans [`STAGE_09_HANDOFF.md`](../90_handoffs/STAGE_09_HANDOFF.md) — sa
section « État actuel (vérité unique) » fait foi pour l'état courant ; ses sections « Nème passe »
sont un journal historique. STAGE 08 reste `STAGE_08_INTEGRATED_IN_MAIN` (inchangée par ce travail).

**STAGE 09 est `STAGE_09_COMPLETE_DRAFT_PR_OPEN`** : les six remédiations cumulées (adaptateur
fail-open, fuite de ressources live, mutation intermittente de placeholder, comptage exact des appels
provider réels, transport de traduction changé de `googletrans` vers le RPC Google Translate Web, et
cette passe -- correction du contrat de wire de ce RPC après audit externe) sont toutes corrigées avec
preuve réelle ou déterministe. Toutes les clauses techniques/canoniques de la Definition of Done sont
satisfaites (migrations, engine, UI, fuzz/failure/load, benchmark de traduction root-causé et remédié,
live primitives + 9/11 groupes de la chaîne complète dans ce sandbox, 31/31 REQ, régressions 01-08,
docs/handoff/state, tests déterministes prouvant qu'aucun candidat corrompu ne peut jamais atteindre
la publication, que l'évidence de comptage d'appels provider est exacte, et que l'adaptateur RPC --
désormais aligné sur le contrat de wire réellement prouvé, pas un contrat inventé -- respecte le même
contrat fail-closed). Le provider de production est `GoogleTranslateRpcCampaignTranslationProvider`,
seule une confirmation externe (smoke réseau réel PUIS benchmark canonique contre ce transport avec le
contrat corrigé, depuis le réseau du product owner) reste ouverte -- une clause
`EXTERNAL_ACCEPTANCE_ITEM` comme la revue humaine, pas un défaut d'implémentation. Le merge reste hors
de portée (`DO NOT MERGE — EXTERNAL AUDIT REQUIRED`). Aucun travail STAGE 10 n'a commencé.
