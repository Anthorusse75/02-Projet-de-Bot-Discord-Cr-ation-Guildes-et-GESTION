# État courant

| Champ | Valeur |
|---|---|
| Current stage | `STAGE_09_COMPLETE_DRAFT_PR_OPEN` -- **restauré** depuis la régression temporaire `STAGE_09_BLOCKED_TRANSLATION_PROVIDER_UNAVAILABLE` (passe précédente) ; voir « Historique des trois remédiations » ci-dessous |
| Last completed/integrated stage | `STAGE_08_MULTILINGUAL_CONTENT_AND_TRANSLATION_TOPOLOGY` |
| Base main (point de divergence de `stage/09-campaigns`) | `c41b61ae96cdb1d767c8d924212a6466b768ed60` — le commit de `main` d'où la branche STAGE 09 a été créée ; ce n'est **pas** nécessairement le HEAD courant de `main` (voir `git rev-parse main` pour l'état à jour) |
| STAGE 08 functional tested code | `592b94bdee713cfb51e236e29cb979ba60e53ac9` |
| STAGE 08 approved final head | `42274836256d2af449678c239c2db4d8e5e6d01d` |
| STAGE 08 merge commit | `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 08 tag | `stage-08-complete` -> `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 09 branch | `stage/09-campaigns`, créée depuis `main` au commit `c41b61ae96cdb1d767c8d924212a6466b768ed60` |
| STAGE 09 HEAD (cette passe) | le commit qui contient exactement ce texte est par construction le dernier commit poussé de cette passe -- voir `git log -1 --format=%H` sur `stage/09-campaigns`, identique au head affiché sur PR #9 après le push de cette passe |
| STAGE 09 PR | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9), ouverte, non mergée, `DO NOT MERGE — EXTERNAL AUDIT REQUIRED` |
| Last migration | `0032_stage_09` ; chaîne `0013_stage_07 → … → 0021_stage_08 → 0022 → … → 0032_stage_09` (32 migrations Stage09) ; tête unique, inchangée cette passe |
| Historique des douze remédiations + clôture d'évidence (mêmes findings externes puis évidence empirique produit owner, PR #9) | (1) Défaut fail-open de l'adaptateur `googletrans` -- corrigé (`raise_exception=True` + vérification défensive du statut HTTP). (2) Fuite de ressources Discord dans la qualification live sur échec -- corrigée (`CleanupRegistry`, nettoyage immédiat, référencé par identité, jamais copié). (3) Mutation intermittente de placeholder côté provider (1/312 mesures `FULL_MASKED_MESSAGE` réelles) -- root-causée et corrigée par une retry bornée avec régénération de placeholders, jamais un relâchement du fail-closed. (4) Comptage sous-évalué des appels provider dans l'évidence du benchmark -- corrigé par un compteur explicite `provider_invocation_count` (port-level). (5) Le transport `googletrans` `/translate_a/single` lui-même s'est révélé spécifiquement indisponible (HTTP 429, prouvé sur deux machines du réseau du product owner) alors qu'une sonde du transport RPC Google Translate Web (`batchexecute`) a réussi (HTTP 200) depuis la même machine -- production basculée vers `GoogleTranslateRpcCampaignTranslationProvider`. (6) Un audit externe a trouvé que le contrat de requête/réponse RPC choisi en (5) avait été inventé plutôt qu'empiriquement prouvé -- corrigé, aligné exactement sur le contrat réel prouvé par le product owner, depuis confirmé empiriquement par un smoke réseau réel (`5 passed in 6.16s`, SHA `d206aa46117bdcfc64b0f60404ba524e7dc53e51`). (7) Le compteur `provider_invocation_count` de (4), bien qu'exact au niveau du port `provider.translate(...)`, sous-comptait encore les vraies tentatives réseau, car l'adaptateur a sa propre reprise bornée interne (`max_attempts=3`) invisible à ce niveau -- corrigé par un mécanisme d'observation `contextvars`-based au niveau transport, `count_transport_attempts()`. (8) Le vrai benchmark canonique contre le code corrigé (SHA `1d71164f5ab24f1585048b3fcc226461d5b2ce1d`, 2285 vraies tentatives HTTP) a rapporté `FAIL` -- 12/12 échecs de production isolés à la classe `url_adversarial`, cause racine forensiquement identifiée (perte d'espace après un placeholder d'URL à une frontière de phrase, jamais le placeholder/l'URL eux-mêmes) et corrigée par une normalisation étroite, source-prouvée, avant restauration. (9) Le product owner a rejoué le benchmark canonique complet contre le code corrigé de (8) (SHA `f19d6616a170386995664de4f92fd65347063ba0`) : **`PASS`**, 2272 vraies tentatives HTTP, `PRODUCTION_FULL_MASKED_MESSAGE_WITH_RETRY` 312/312 (100%) -- ce résultat remplace le `FAIL` de (8) comme évidence canonique COURANTE sans jamais l'effacer. Un script reproductible génère un pack de 36 traductions réelles (les 12 paires dirigées × 3 classes) pour la revue sémantique HUMAINE -- tous les champs de jugement restent intentionnellement vides, jamais assignés par l'agent d'implémentation ni par un outil IA. (10) **Cette passe** : le product owner a exécuté ce script pour de vrai contre le SHA `92fa8aae18542416790767909e45a755ee6e321e` (36 appels réels, 36/36 intégrité de placeholders `PASS`) -- mais le texte restauré COMPLET a révélé un **nouveau défaut de production réel, non détectable par le benchmark d'intégrité** : les 12/12 lignes `mixed_technical_and_linguistic` perdent l'espace après la ponctuation adjacente à deux frontières de placeholder supplémentaires (`USER_MENTION`+`!`, `TIMESTAMP`+`.`) -- même classe de défaut que (8), mais qui ne fait PAS échouer `validate_reparsed_structure()`. **Corrigé** : la fonction de (8) est généralisée en `restore_source_proven_protected_boundary_spacing()`, pilotée par une table explicite à trois combinaisons empiriquement prouvées (`URL`+`.`, `USER_MENTION`+`!`, `TIMESTAMP`+`.`), jamais élargie sans preuve réelle. (11) **Cette passe** : après audit externe, le product owner a exécuté pour de vrai la qualification réseau ciblée de (10) contre le SHA `450bf3b928c931a1355078d3ba3305f8eb8b3ae6` : **11/12 PASS, 1/12 FAIL** (EN→FR). Les 12/12 directions rapportaient une intégrité de placeholders `PASS` (0 erreur, 1 tentative HTTP chacune, 0 reprise), mais la frontière USER_MENTION+`!` restait collée dans la sortie EN→FR exacte : `<@123456789012345678>\xa0!Votre événement...`. **Root cause** : Google a correctement rendu la typographie française avec un NO-BREAK SPACE U+00A0 avant `!` (typographie légitime, PAS une corruption) -- mais le scanner de frontière de DID ne reconnaissait que l'espace ASCII/la tabulation, donc n'atteignait jamais le `!` pour détecter l'espace toujours manquant après lui. **Corrigé** : `_is_horizontal_whitespace()` élargi à TABULATION OU catégorie Unicode "Zs", couvrant NBSP/NARROW NBSP/tout séparateur Unicode, tout en excluant les sauts de ligne -- reconnaissance uniquement, aucun espace pré-ponctuation jamais réécrit. Matrice `SUPPORTED_BOUNDARY_PUNCTUATION` inchangée. 10 nouveaux tests déterministes le prouvent. (12) **Cette passe** : le product owner a régénéré le pack de 36 échantillons contre le SHA `6acfcc2460850e0507c71be74a51584f150ded18` (correctif Root cause 9 inclus) : **36/36 intégrité de placeholders `PASS`, 0 erreur, 0 reprise**, et tous les échantillons `mixed_technical_and_linguistic` montrent désormais un espacement de frontière correct -- corroboration réelle en production. Après examen des 36 échantillons, **le Product Owner ACCEPTE la qualité de traduction Stage 09 au niveau du projet** (décision documentée dans `docs/90_handoffs/evidence/stage09/HUMAN_SEMANTIC_REVIEW.md` § « Acceptation Product Owner ») -- explicitement une acceptation de PROJET, pas une certification linguistique professionnelle ligne par ligne : le tableau « Verdict humain » de chaque échantillon reste intentionnellement vide, jamais rempli ni fabriqué. Formulation occasionnellement moins native acceptée comme non bloquante (EN→FR échantillons 16/17 compréhensibles mais moins naturels, échantillon 18 satisfaisant). **La revue sémantique humaine n'est plus un gate bloquant pour Stage 09.** Voir `STAGE_09_HANDOFF.md` § « État actuel » pour le détail complet de chacune |
| **Transport de traduction (changé passe -5, contrat de wire corrigé cette passe)** | La conclusion antérieure (« provider `googletrans` globalement indisponible depuis ce sandbox, opérationnel depuis le réseau du product owner ») avait été affinée par une évidence empirique plus précise : le transport `/translate_a/single` spécifiquement échoue avec HTTP 429 **même depuis le réseau du product owner**, reproduit sur une seconde machine indépendante. En revanche, l'autre endpoint web non officiel de Google -- le RPC `batchexecute` (id `MkEWBc`) que `translate.google.com` utilise lui-même -- a répondu HTTP 200 avec une vraie traduction depuis la même machine où `googletrans` échoue. Production, qualification live et benchmark utilisent tous `GoogleTranslateRpcCampaignTranslationProvider` (module `did.translation.google_translate_rpc_adapter`) ; `googletrans` reste dans le dépôt uniquement comme code/dépendance historique et comparatif, jamais construit sur un chemin réel. **Cette passe** : un audit externe a établi que le contrat de requête/réponse effectivement codé pour ce module était inventé (paramètres de requête fabriqués, forme de réponse devinée), pas le contrat réel prouvé par la sonde du product owner -- seuls l'endpoint et le RPC id l'étaient. Corrigé : requête et parsing de réponse désormais alignés exactement sur ce contrat prouvé (voir `STAGE_09_HANDOFF.md` Root cause 5). Le seul blocage restant est une confirmation externe : le product owner doit rejouer le petit smoke réseau réel PUIS le benchmark canonique contre ce transport, avec le contrat désormais corrigé (voir « Commande de confirmation finale » ci-dessous) |
| STAGE 09 implementation (inchangée structurellement) | Fondations WP1-WP13, remédiation externe (17 findings), orchestration WP12 réelle, connexion runtime réelle, API HTTP complète (WP14), réconciliation de livraison câblée, surfaces d'authoring produit complètes en UI/API, matrice de qualification live Discord (WP16/mission section 20). **Résultat REQ-MSG inchangé : 31/31 `IMPLEMENTED`, 0 `PARTIALLY_IMPLEMENTED`, 0 `NOT_STARTED`** |
| Data model | Inchangé cette passe ; voir migrations `0022`-`0032_stage_09` |
| Tests status | Canonique inchangé : `validate_stage.py 09` (défaut), `08`, `--profile failure-injection`, `--profile e2e` : **PASS**. Toutes les suites des passes précédentes inchangées, dont `test_stage09_human_semantic_review_pack.py` (23 tests déterministes, offline) et `test_stage09_parser_protector.py`'s `TestProtectedBoundarySpacingSourceProvenRepair` (19 tests, Root cause 8). **Cette passe (Root cause 9)** : `test_stage09_parser_protector.py` gagne `TestUnicodeHorizontalWhitespaceAtProtectedPunctuationBoundaries` (10 nouveaux tests : NBSP, NARROW NBSP, régressions ASCII/no-pre-space, boundary déjà correcte inchangée, préservation byte-pour-byte de l'espace Unicode, saut de ligne jamais horizontal, régressions URL/TIMESTAMP avec espace Unicode, forme française exacte bout-en-bout) ; `test_stage09_rendering.py` gagne `TestNbspBoundarySpacingDoesNotTriggerAnArtificialRetry` (1 test). Le scanner du fichier réseau ciblé `test_stage09_translation_network_mixed_technical.py` est aussi rendu Unicode-aware (indépendamment du code de production). `pyproject.toml` : `per-file-ignore` `RUF001` ajouté, scopé au seul fichier de test qui exerce délibérément des caractères Unicode d'espace horizontal réels. Toujours aucun réseau requis dans la suite déterministe, `httpx.MockTransport` simule le transport HTTP là où pertinent -- voir « Validation canonique » de `STAGE_09_HANDOFF.md` pour le décompte PASS exact de cette passe. `ruff`/`mypy`/`check_secrets`/`git diff --check` : PASS |
| **Correction de comptage port-level (passe -2, defect trouvé par audit externe)** | `scripts/run_translation_benchmark.py::_run_one_production()`/`_run_one()` enregistraient `retry_count`/le nombre de tentatives mais `_summarize()` dérivait `provider_invocations` de `record.segment_count` -- qui ne reflète que la mesure structurelle de la tentative GAGNANTE finale, sous-comptant silencieusement les vrais appels réseau dès qu'une reprise avait lieu (ex. tentative 1 échoue l'intégrité + tentative 2 réussit = 2 vrais appels provider, mais `segment_count` de la tentative gagnante ne vaut que 1) ou qu'une stratégie multi-segments échouait en cours de route (`segment_count=0` sur l'enregistrement d'erreur effaçait aussi les appels déjà réellement effectués). **Corrigé à l'époque** : nouveau champ explicite `provider_invocation_count`, incrémenté à l'instant même où chaque appel réel à `provider.translate(...)` (le PORT haut niveau) est tenté. **Mais voir la ligne suivante : cette correction s'est révélée elle-même incomplète.** |
| **Correction de comptage niveau transport (cette passe, deuxième audit externe)** | `GoogleTranslateRpcCampaignTranslationProvider.translate()` a sa PROPRE reprise bornée interne (`max_attempts=3`, distincte de la reprise d'intégrité ci-dessus) : un seul appel `translate()` peut donc silencieusement générer jusqu'à 3 vrais POST HTTP avant de réussir ou d'échouer -- le test déterministe HTTP-429-puis-succès de l'adaptateur le prouvait déjà. Compter les appels `translate()` (la correction précédente) sous-comptait donc les vraies tentatives réseau jusqu'à 3x dès que la reprise interne de l'adaptateur s'engageait. **Corrigé** : nouveau mécanisme `did.translation.google_translate_rpc_adapter.count_transport_attempts()` -- un `contextvars.ContextVar` (jamais un attribut mutable partagé sur l'instance du provider, qui deviendrait silencieusement incorrect si des mesures concurrentes se chevauchaient un jour) notifié immédiatement avant chaque vrai `await client.post(...)`, jamais sur un fast-fail de circuit breaker, un sleep de backoff, ou le parsing. Le benchmark enveloppe désormais chaque mesure complète dans ce contexte et dérive `provider_invocation_count` du delta réel observé. Le nom du champ JSON reste inchangé (aucune migration de schéma) mais désigne désormais honnêtement le nombre exact de vraies tentatives HTTP RPC. 10 nouveaux tests déterministes le prouvent contre le vrai adaptateur via `httpx.MockTransport` (voir ci-dessus). |
| Root-cause de la mutation de placeholder | Analysée mais non "corrigée" par un changement d'encodage : le placeholder `DIDPH{index:04d}Q{8 hex}ZH` a été transformé par le provider en un AUTRE token de même forme (un chiffre hex modifié), sur du contenu mêlant code inline et bloc de code fenced (`inline_code_and_block_mixed`, ES→FR). Le fail-closed existant a correctement rejeté ce candidat (`IntegrityViolation`) -- ce n'était PAS une brèche de sécurité, mais une lacune de disponibilité (aucune reprise). Le format de placeholder lui-même n'a pas été changé (préserverait l'invariant existant sans le justifier par la mesure) ; la reprise bornée avec régénération est la remédiation choisie, appuyée par la preuve réelle qu'un retest à 5 tentatives avec placeholders neufs a réussi 5/5 |
| **Root-cause de l'échec `url_adversarial` (cette passe)** | Le vrai benchmark canonique RPC (SHA `1d71164f5ab24f1585048b3fcc226461d5b2ce1d`, comptage transport-exact) a rapporté `FAIL` : `PRODUCTION_FULL_MASKED_MESSAGE_WITH_RETRY` 300/312 (0.9615), 2285 vraies tentatives HTTP RPC au total. **12/12 échecs de production == classe `url_adversarial`, les 12 paires de langues dirigées, chaque échec persistant après la seconde tentative d'intégrité.** Reproduction forensique réelle (un seul appel EN→FR) : le placeholder d'URL et l'URL elle-même sont préservés byte pour byte -- Google supprime seulement l'espace entre le "." final de phrase qui suit immédiatement le placeholder et le mot cible suivant (`DIDPH.... . See also` → `DIDPH....Voir aussi`). L'URL restaurée absorbe alors lexicalement le mot suivant, et `validate_reparsed_structure()` rejette correctement ce token comme non présent dans la source -- ce n'est ni un placeholder inventé/perdu, ni un défaut du parseur d'URL, ni une mutation d'URL par Google. **Corrigé** : `did.messaging.protector.restore_source_proven_url_boundary_spacing()`, une normalisation pure intégrée dans `validate_and_restore()` après la validation exacte du multi-ensemble de placeholders mais avant la restauration -- restaure UN espace uniquement quand le `ProtectionResult.masked_text` **source** prouve déjà que la frontière `<placeholder>. ` existait avant traduction, jamais inféré depuis la langue cible ou la syntaxe d'URL seule. Scope strictement limité à ce que l'évidence prouve : "." + espace, uniquement `ProtectedKind.URL`. Le regex d'URL, `validate_reparsed_structure()`, et le reste du fail-closed restent inchangés -- une URL genuinement mutée ou une hallucination restent rejetées. |
| Translation benchmark status | Évidence committée historique (ce sandbox, préservée telle quelle, jamais effacée) : `BLOCKED`, 1248/1248 mesures en échec réel contre l'ancien transport `googletrans`. Évidence historique rapportée (jamais fabriquée/substituée, préservée telle quelle) : (a) 1950 appels réels/0 erreur contre `googletrans` opérationnel à l'époque, `FULL_MASKED_MESSAGE` 311/312 (99.68%) ; (b) transport `googletrans` devenu indisponible (429), RPC probé HTTP 200 ; (c) vrai benchmark canonique RPC contre le code D'AVANT le correctif `url_adversarial` : `FAIL`, 2285 tentatives HTTP, 300/312, 12/12 échecs == `url_adversarial`. **(d)** vrai benchmark canonique RPC contre le code APRÈS le correctif (SHA `f19d6616a170386995664de4f92fd65347063ba0`) : **`PASS`**, 2272 tentatives HTTP réelles, `PRODUCTION_FULL_MASKED_MESSAGE_WITH_RETRY` **312/312 (100%)**, 0 erreur, 0 reprise. Committé à `docs/90_handoffs/evidence/stage09/translation-benchmark-PASS-f19d6616a170.json` (résumé exact rapporté, `records` complet non copiable depuis ce sandbox -- `artifacts/` gitignored). Ce PASS remplace le FAIL (c) comme évidence courante sans jamais l'effacer. **(e)** ce PASS mesure l'intégrité du multi-ensemble de placeholders, PAS la qualité de présentation de l'espacement -- le pack de revue humaine réel (généré contre le SHA `92fa8aae18542416790767909e45a755ee6e321e`) a montré que 12/12 lignes `mixed_technical_and_linguistic` perdent l'espace après la ponctuation à deux frontières (`USER_MENTION`+`!`, `TIMESTAMP`+`.`) que ce PASS ne pouvait pas voir -- voir Root cause 8. **(f) NOUVEAU cette passe** : la qualification réseau ciblée du correctif de (e) a été exécutée réellement contre le SHA `450bf3b928c931a1355078d3ba3305f8eb8b3ae6` : **11/12 PASS, 1/12 FAIL** (EN→FR) -- voir Root cause 9 pour la cause exacte (reconnaissance Unicode du scanner de frontière) et le correctif. Preuve committée à `docs/90_handoffs/evidence/stage09/mixed-technical-network-qualification-450bf3b9-FAIL.json` |
| **Commande de confirmation finale -- benchmark d'intégrité CONFIRMÉ, revue de traduction ACCEPTÉE au niveau projet** | Étape 1 (smoke) : `5 passed in 6.16s`. Étape 2 (`python scripts/validate_stage.py 09 --profile translation-benchmark --allow-network`) : **`PASS`**, contre le SHA `f19d6616a170386995664de4f92fd65347063ba0`. **Ne PAS rejouer ce benchmark à nouveau** (~2272 appels). Étape 3 (qualification ciblée `mixed_technical_and_linguistic`, Root cause 8) : exécutée réellement contre le SHA `450bf3b928c931a1355078d3ba3305f8eb8b3ae6` -- 11/12 PASS, 1/12 FAIL, cause exacte et correctif intégrés (Root cause 9). Étape 4 (**cette passe**) : le product owner a régénéré le pack de 36 échantillons contre le SHA `6acfcc2460850e0507c71be74a51584f150ded18` (correctif inclus) -- 36/36 intégrité `PASS`, espacement correct sur tous les échantillons -- et **accepte la qualité de traduction au niveau projet** (voir `HUMAN_SEMANTIC_REVIEW.md` § « Acceptation Product Owner »). Le fichier de qualification réseau ciblé dédié (`test_stage09_translation_network_mixed_technical.py`, `DID_ALLOW_NETWORK=1 uv run pytest backend/tests/network/test_stage09_translation_network_mixed_technical.py -m translation_network -v -s`) reste committé et exécutable indépendamment pour toute future régression, mais n'est plus un gate bloquant maintenant que l'acceptation projet est enregistrée. **Ne PAS régénérer le pack de 36 échantillons à nouveau** pour ce sujet. |
| Discord live status | Évidence committée (ce sandbox, inchangée cette passe -- aucun accès réseau fonctionnel disponible ici pour la rejouer) : `validate_discord_live_stage09.py` 5/5 PASS ; `validate_discord_live_stage09_full_chain.py` (11 groupes) 9/11 groupes 100% PASS (rien lié à la traduction), `translation_group_provider_boundary` 100% PASS, `translation_group_did_fanout` échoue honnêtement sur ce qui dépend d'une traduction obtenue. La correction de mutation de placeholder n'a donc pas encore été vérifiée contre CE chemin live spécifique dans ce sandbox -- elle l'a été de façon déterministe (16 tests) et par le product owner au niveau benchmark |
| Nettoyage du sandbox live (passe précédente) | `CleanupRegistry` (`scripts/_stage09_cleanup_registry.py`) corrige la fuite de ressources sur échec ; `scripts/cleanup_stage09_orphan_fixtures.py` a supprimé les 50 orphelins historiques (0 échec, 0 restant vérifié). Inchangé cette passe |
| GitHub publication | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9) ouverte contre `main`, non mergée ; branche `stage/09-campaigns` poussée |
| Next stage | `STAGE_10` reste `NOT_STARTED` et interdite tant que STAGE 09 n'est pas intégrée dans `main` |
| Known limitations (toutes `EXTERNAL_ACCEPTANCE_ITEM`) | (A) **Revue de qualité de traduction** : **ACCEPTÉE par le Product Owner au niveau projet cette passe** -- voir `docs/90_handoffs/evidence/stage09/HUMAN_SEMANTIC_REVIEW.md` § « Acceptation Product Owner ». Acceptation de PROJET (contenu jugé publiable), pas une certification linguistique professionnelle ligne par ligne -- le tableau « Verdict humain » de chaque échantillon reste intentionnellement vide. Pack régénéré contre le SHA `6acfcc2460850e0507c71be74a51584f150ded18` (correctif Root cause 9 inclus) : 36/36 intégrité `PASS`, 0 erreur, espacement de frontière correct sur tous les échantillons `mixed_technical_and_linguistic`. **N'est plus un gate bloquant.** (B) **Qualification réseau/benchmark d'intégrité de placeholders** : **CLOSE** -- smoke réel et benchmark canonique complet tous deux `PASS` contre le SHA `f19d6616a170386995664de4f92fd65347063ba0`, aucune commande réseau supplémentaire requise. (C) **Provider de traduction tiers réellement présent dans le sandbox** (distinct du chemin de traduction direct propre à DID) : `EXTERNAL_SANDBOX_CAPABILITY_NOT_AVAILABLE`, inchangé. (D) **`UNKNOWN_OUTCOME` réel non reproductible à la demande contre Discord** : `NOT_SAFELY_REPRODUCIBLE_LIVE`, inchangé. |

Le détail d'architecture, les décisions de conception, les 17 findings de remédiation externe, et le
détail pass-par-pass sont dans [`STAGE_09_HANDOFF.md`](../90_handoffs/STAGE_09_HANDOFF.md) — sa
section « État actuel (vérité unique) » fait foi pour l'état courant ; ses sections « Nème passe »
sont un journal historique. STAGE 08 reste `STAGE_08_INTEGRATED_IN_MAIN` (inchangée par ce travail).

**STAGE 09 est `STAGE_09_COMPLETE_DRAFT_PR_OPEN`** : les douze remédiations cumulées (adaptateur
fail-open, fuite de ressources live, mutation intermittente de placeholder, comptage port-level des
appels provider, transport de traduction changé de `googletrans` vers le RPC Google Translate Web,
correction du contrat de wire de ce RPC, comptage rendu exact au niveau TRANSPORT, la correction de la
frontière d'espacement après un placeholder d'URL root-causée par le vrai benchmark canonique du
product owner, la reconfirmation `PASS` de ce benchmark, la généralisation de cette même correction à
deux frontières supplémentaires (`USER_MENTION`+`!`, `TIMESTAMP`+`.`) root-causée par le pack de revue
humaine réel, l'élargissement de la reconnaissance d'espace horizontal du scanner de frontière à tout
séparateur Unicode root-causé par la qualification réseau ciblée réelle, et -- **cette passe** -- la
régénération du pack de 36 échantillons contre le correctif Unicode et son acceptation par le Product
Owner) sont toutes corrigées avec preuve réelle ou déterministe. Le benchmark canonique d'INTÉGRITÉ DE
PLACEHOLDERS (SHA `f19d6616a170386995664de4f92fd65347063ba0`) reste `PASS`, 2272 tentatives HTTP
réelles, production 312/312 (100%). Le pack de 36 échantillons régénéré contre le SHA
`6acfcc2460850e0507c71be74a51584f150ded18` (correctif Root cause 9 inclus) rapporte **36/36 intégrité
de placeholders `PASS`, 0 erreur**, et tous les échantillons `mixed_technical_and_linguistic`
montrent un espacement de frontière correct. **Après examen des 36 échantillons, le Product Owner
ACCEPTE la qualité de traduction Stage 09 au niveau du projet** -- une acceptation de PROJET
(contenu jugé publiable en pratique), explicitement PAS une certification linguistique professionnelle
ligne par ligne : le tableau « Verdict humain » de chaque échantillon reste intentionnellement vide,
jamais rempli ni fabriqué, conformément à l'exigence de ne jamais assigner de score par l'agent
d'implémentation ou un outil IA. Toutes les clauses techniques/canoniques de la Definition of Done
sont satisfaites (migrations, engine, UI, fuzz/failure/load, benchmark de traduction, live primitives
+ 9/11 groupes de la chaîne complète dans ce sandbox, 31/31 REQ, régressions 01-08, docs/handoff/state,
tests déterministes prouvant qu'aucun candidat corrompu ne peut jamais atteindre la publication). Le
provider de production est `GoogleTranslateRpcCampaignTranslationProvider`. Il ne reste que deux
limitations honnêtement externes, ni l'une ni l'autre bloquante pour la traduction : (C) participation
live d'un provider de traduction tiers réel attaché à un Translation Group,
`EXTERNAL_SANDBOX_CAPABILITY_NOT_AVAILABLE` ; (D) reproduction sûre d'un `UNKNOWN_OUTCOME` réel contre
Discord, `NOT_SAFELY_REPRODUCIBLE_LIVE`. Le merge reste hors de portée
(`DO NOT MERGE — EXTERNAL AUDIT REQUIRED`). Aucun travail STAGE 10 n'a commencé.
