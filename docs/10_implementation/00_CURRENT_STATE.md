# État courant

| Champ | Valeur |
|---|---|
| Current stage | `STAGE_09_COMPLETE_DRAFT_PR_OPEN` |
| Last completed/integrated stage | `STAGE_08_MULTILINGUAL_CONTENT_AND_TRANSLATION_TOPOLOGY` |
| Base main (point de divergence de `stage/09-campaigns`) | `c41b61ae96cdb1d767c8d924212a6466b768ed60` — le commit de `main` d'où la branche STAGE 09 a été créée ; ce n'est **pas** nécessairement le HEAD courant de `main` (voir `git rev-parse main` pour l'état à jour) |
| STAGE 08 functional tested code | `592b94bdee713cfb51e236e29cb979ba60e53ac9` |
| STAGE 08 approved final head | `42274836256d2af449678c239c2db4d8e5e6d01d` |
| STAGE 08 merge commit | `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 08 tag | `stage-08-complete` -> `d6a8425cff6606cace2ac89705b71519b6a308b1` |
| STAGE 09 branch | `stage/09-campaigns`, créée depuis `main` au commit `c41b61ae96cdb1d767c8d924212a6466b768ed60` |
| STAGE 09 HEAD (cette passe de clôture évidence) | le commit qui contient exactement ce texte est par construction le dernier commit poussé de cette passe -- voir `git log -1 --format=%H` sur `stage/09-campaigns`, identique au head affiché sur PR #9 après le push de cette passe |
| STAGE 09 PR | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9), ouverte, non mergée, `DO NOT MERGE — EXTERNAL AUDIT REQUIRED` |
| Last migration | `0032_stage_09` ; chaîne `0013_stage_07 → … → 0021_stage_08 → 0022 → … → 0032_stage_09` (32 migrations Stage09) ; tête unique, rehearsal `downgrade base → upgrade head → downgrade 0001_stage_01 → upgrade head` validé sur PostgreSQL réel |
| STAGE 09 implementation | Fondations WP1-WP13 complètes et testées, remédiation externe (17 findings) intégrée, root-cause d'intégrité de traduction corrigée, orchestration WP12 réelle (`fan_out_occurrence`/`run_scheduler_tick`/`consume_event_for_trigger`), connexion au runtime réel (`did.runtime.py` exécute le Campaign Engine dans les process scheduler/worker de production), API HTTP Stage09 complète (WP14), réconciliation de livraison câblée dans le runtime réel, surfaces d'authoring produit Stage09 complètes en UI/API (MessageModel/embed/composants, variables typées, glossaire, trigger d'événement, rétention, édition/suppression possédée, requeue/intervention), et la matrice de qualification live Discord complète (WP16/mission section 20), y compris les scénarios Translation Group avec le vrai `GoogletransCampaignTranslationProvider` de production. **Résultat : 31/31 REQ-MSG-001..031 `IMPLEMENTED`, 0 `PARTIALLY_IMPLEMENTED`, 0 `NOT_STARTED`.** Voir `STAGE_09_HANDOFF.md` § « État actuel (vérité unique) » pour le détail à jour ; les sections « Nème passe » de ce handoff sont un journal historique, pas l'état courant. |
| Data model | Tables STAGE 09 : header Control-Plane user-owned (RLS `owner_discord_user_id`) séparé des tables Guild tenant-scoped (RLS `guild_id`, FK composite) ; voir migrations `0022`-`0032_stage_09` |
| Tests status | `pytest --collect-only -q backend/tests -k stage09` : **576 tests collectés** ; décomptes PASS exacts de la suite complète (`tests/unit`, `tests/integration`, frontend `vitest`/`playwright`), `ruff`, `mypy`, `openapi:check`, `check_secrets.py` : voir le run canonique `scripts/validate_stage.py 09` de cette passe (rapport sanitisé sous `artifacts/test-evidence/stage-09/`, non gitignoré pour les extraits committés sous `docs/90_handoffs/evidence/stage09/`) |
| Traceability | **31** IDs `IMPLEMENTED`, **0** `PARTIALLY_IMPLEMENTED`, **0** `NOT_STARTED` sur `REQ-MSG-001..031` — matrice complète fermée, inchangée depuis la passe précédente (aucun REQ n'a régressé). Chaque ligne pointe vers du code de production, des tests et une preuve reproductible dans `00_REQUIREMENTS_TRACEABILITY.md` / `STAGE09_REQUIREMENTS_CHECKLIST_LOCAL.md` |
| Discord live status | Point d'entrée canonique unique : `python scripts/validate_stage.py 09 --include-discord-live` exécute désormais **à la fois** `scripts/validate_discord_live_stage09.py` (adaptateur-primitives, 5/5 scénarios PASS) **et** `scripts/validate_discord_live_stage09_full_chain.py` (matrice produit complète, 11 groupes/59 vérifications PASS, incl. les scénarios Translation Group SOURCE_ONLY/DID_TRANSLATED_FANOUT-FR-EN-DE-ES/SELECTED_LANGUAGES/variante-approuvée avec le vrai provider `googletrans` de production, et un groupe frontière-provider prouvant qu'un provider-binding non-`DISABLED` bloque correctement le fan-out sans mutation Discord) — dans la même exécution, rapports séparés, ni l'un ni l'autre ne se substitue à l'autre. Preuve sanitisée committée : `docs/90_handoffs/evidence/stage09/discord-live-stage09.json` et `docs/90_handoffs/evidence/stage09/discord-live-stage09-full-chain.json` |
| GitHub publication | Draft PR [#9](https://github.com/Anthorusse75/02-Projet-de-Bot-Discord-Cr-ation-Guildes-et-GESTION/pull/9) ouverte contre `main`, non mergée ; branche `stage/09-campaigns` poussée |
| Next stage | `STAGE_10` reste `NOT_STARTED` et interdite tant que STAGE 09 n'est pas intégrée dans `main` |
| Known limitations (toutes externes à l'implémentation technique) | (A) **Revue sémantique humaine** : `PENDING_HUMAN_REVIEW` — aucune évaluation humaine n'a eu lieu, aucun score fabriqué ; pack d'échantillons FR/EN/DE/ES préparé, `docs/90_handoffs/evidence/stage09/human-semantic-review-pack.md`. (B) **Provider de traduction tiers réellement présent dans le sandbox** (distinct du provider `googletrans` propre à DID, lui-même exercé en live) : `EXTERNAL_SANDBOX_CAPABILITY_NOT_AVAILABLE` — aucun bot externe de ce type n'existe dans ce sandbox ; l'état bloquant est prouvé en live à sa place. (C) **`UNKNOWN_OUTCOME` réel non reproductible à la demande contre Discord** : `NOT_SAFELY_REPRODUCIBLE_LIVE`, couvert par injection de panne déterministe en intégration. (D) **Fragilité externe, non un défaut Stage09** : le `googletrans==4.0.2` réel renvoie actuellement un texte identique à l'entrée (écho) dans ce sandbox plutôt qu'une traduction — round-trip API réussi (`detected_source_language` correct), fragilité pré-existante de cette bibliothèque non officielle contre l'endpoint Google actuel, hors périmètre de correction (« ne pas redesigner »). N'affecte aucune assertion sous la responsabilité du code DID (routage/comptage/contrat de non-fallback silencieux), tous prouvés live à 100% ; chaque occurrence est enregistrée honnêtement dans l'évidence committée, jamais masquée. |

Le détail d'architecture, les décisions de conception, les 17 findings de remédiation externe, et le
détail pass-par-pass (root-cause REQ-MSG-025, fencing strict, worker réel, orchestration WP12
complète, connexion runtime réelle, API HTTP complète, ancestry producing-side, UI Translation Group,
surfaces d'authoring produit, réconciliation câblée, matrice live complète) sont dans
[`STAGE_09_HANDOFF.md`](../90_handoffs/STAGE_09_HANDOFF.md) — sa section « État actuel (vérité unique) »
fait foi pour l'état courant ; ses sections « Nème passe » sont un journal historique. STAGE 08 reste
`STAGE_08_INTEGRATED_IN_MAIN` (inchangée par ce travail). STAGE 09 est `STAGE_09_COMPLETE_DRAFT_PR_OPEN` :
toutes les clauses techniques de la Definition of Done sont satisfaites avec preuve réelle (migrations,
engine, UI, fuzz/failure/load, corpus réel, live primitives + chaîne complète, 31/31 REQ, régressions
01-08, docs/handoff/state) ; seules des clauses légitimement externes à toute passe technique restent
ouvertes (revue humaine, provider tiers réellement présent, `UNKNOWN_OUTCOME` réel) — voir le tableau
ci-dessus et l'audit de Definition of Done à trois voies dans `STAGE_09_HANDOFF.md`. Le merge lui-même
reste soumis à revue externe (`DO NOT MERGE — EXTERNAL AUDIT REQUIRED`). Aucun travail STAGE 10 n'a
commencé.
